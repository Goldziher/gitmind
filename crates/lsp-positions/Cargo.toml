# License: MIT OR Apache-2.0 (see LICENSE-MIT / LICENSE-APACHE — upstream copyright retained).
[package]
name = "basemind-lsp-positions"
version = "0.3.4"
description = "LSP-compatible character positions (maintained basemind fork, tree-sitter 0.26 port)"
homepage = "https://github.com/Goldziher/basemind"
repository = "https://github.com/Goldziher/basemind"
readme = "README.md"
license = "MIT OR Apache-2.0"
authors = ["Na'aman Hirschfeld <nhirschfeld@gmail.com>"]
edition = "2024"

[lib]
name = "lsp_positions"
test = false

[features]
bincode = ["dep:bincode"]
tree-sitter = ["dep:tree-sitter"]

[dependencies]
# 3.0.0 is a placeholder crate containing an unconditional compile_error!; 2.0.1 is the latest usable release. ~keep
bincode = { version = "=2.0.1", optional = true }
memchr = "2.8"
serde = { version = "1", features = ["derive"], optional = true }
tree-sitter = { version = "0.26", optional = true }
unicode-segmentation = { version = "1.13" }

[lints]
workspace = true
