{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "ConfigV1",
  "type": "object",
  "properties": {
    "$schema": {
      "type": "string"
    },
    "scan": {
      "$ref": "#/$defs/ScanConfig",
      "default": {
        "include": [
          "**/*"
        ],
        "exclude": [
          "**/target/**",
          "**/node_modules/**",
          "**/dist/**",
          "**/.venv/**",
          "**/.basemind/**",
          "**/.git/**",
          "**/bazel-out/**",
          "**/bazel-bin/**",
          "**/bazel-testlogs/**",
          "**/bazel-*/**"
        ],
        "respect_gitignore": true,
        "follow_symlinks": false,
        "max_file_bytes": 2097152,
        "skip_submodules": true,
        "eager_l2": true,
        "extra_roots": []
      }
    },
    "code_intel": {
      "$ref": "#/$defs/CodeIntelConfig",
      "default": {
        "precise_resolution": true
      }
    },
    "watch": {
      "$ref": "#/$defs/WatchConfig",
      "default": {
        "debounce_ms": 250,
        "live_l2": false
      }
    },
    "cache": {
      "$ref": "#/$defs/CacheConfig",
      "default": {
        "file_map_lru": 256
      }
    },
    "mcp": {
      "$ref": "#/$defs/McpConfig",
      "default": {
        "transport": "stdio"
      }
    },
    "languages": {
      "type": "object",
      "additionalProperties": {
        "$ref": "#/$defs/LanguageConfig"
      },
      "default": {}
    },
    "documents": {
      "$ref": "#/$defs/DocumentsConfig",
      "default": {
        "enabled": true,
        "mime_allowlist": [],
        "extension_denylist": [],
        "max_chunks_per_document": 2000,
        "max_characters": 1000,
        "overlap": 200,
        "embedding_preset": "balanced",
        "embed": true,
        "embed_exclude": [],
        "extract_archives": false,
        "language": {
          "auto_detect": true,
          "min_confidence": 0.8,
          "detect_multiple": false,
          "preferred_languages": []
        },
        "reranker": {
          "enabled": false,
          "preset": "bge-reranker-base",
          "top_k": 10
        },
        "keywords": {
          "enabled": false,
          "algorithm": "yake",
          "max_keywords": 10,
          "min_score": 0.0,
          "ngram_range": [
            1,
            3
          ],
          "yake_params": null,
          "rake_params": null
        },
        "ner": {
          "enabled": false,
          "backend": "onnx",
          "model": null,
          "categories": [],
          "custom_labels": []
        },
        "summarization": {
          "enabled": false,
          "strategy": "extractive"
        },
        "ocr": {
          "backend": "tesseract",
          "languages": [
            "eng"
          ]
        },
        "output": {
          "format": "json"
        },
        "embed_max_threads": 0
      }
    },
    "resources": {
      "description": "Resource-governance knobs: scanner / embedder thread caps, document\nconcurrency, embed batch size, footprint ceiling, and the document model\nprofile. Bounds basemind's footprint on constrained machines.",
      "$ref": "#/$defs/ResourcesConfig",
      "default": {
        "scan_threads": 0,
        "embed_threads": 0,
        "max_concurrent_documents": 0,
        "embed_batch_size": 32,
        "max_footprint_mb": 0,
        "document_models": "full"
      }
    },
    "code_search": {
      "description": "Semantic code-search tier: chunk + embed source for the `code` tool's `semantic` mode.\nInert unless the `code-search` cargo feature is compiled in.",
      "$ref": "#/$defs/CodeSearchConfig",
      "default": {
        "enabled": true,
        "max_characters": 1500,
        "overlap": 200,
        "embed": false,
        "embed_exclude": [],
        "reranker": {
          "enabled": false,
          "preset": "bge-reranker-base",
          "top_k": 10
        }
      }
    },
    "memory": {
      "$ref": "#/$defs/MemoryConfig",
      "default": {
        "enabled": true,
        "scope_strategy": "git_remote_with_fallback",
        "default_visibility": "group"
      }
    },
    "comms": {
      "$ref": "#/$defs/CommsConfig",
      "default": {
        "enabled": true,
        "agent_id": null,
        "idle_timeout_secs": 1800,
        "max_messages_per_room": 1000,
        "retention_secs": 604800,
        "max_rooms": 256,
        "workspace_root": null
      }
    },
    "crawl": {
      "$ref": "#/$defs/CrawlConfig",
      "default": {
        "respect_robots_txt": true,
        "max_pages": 32,
        "max_depth": 2,
        "max_body_size": 4194304,
        "user_agent": "basemind/0.23.1 (+https://github.com/Goldziher/basemind)",
        "allow_private_network": false
      }
    },
    "shells": {
      "description": "Visual / headless agent-shell presentation config. Consumed by the `shells` feature's\nvisual launcher; the schema is stable regardless of feature gating.",
      "$ref": "#/$defs/ShellsConfig",
      "default": {
        "enabled": true,
        "visual": "current",
        "terminal": "auto",
        "default_cols": 200,
        "default_rows": 50,
        "keep_on_exit": true
      }
    },
    "llm": {
      "description": "Shared LLM configuration. Consumed by reranker-llm, ner-llm, summarization-llm,\nVLM OCR, and any future LLM-backed capability. Off by default — leaving\n`api_key` `Unset` short-circuits any LLM-backed feature.",
      "$ref": "#/$defs/LlmConfig",
      "default": {
        "model": "",
        "api_key": null,
        "base_url": null,
        "temperature": null,
        "timeout_secs": null,
        "max_retries": null,
        "max_tokens": null
      }
    },
    "agent": {
      "description": "The `[agent]` table, accepted here but interpreted elsewhere.\n\nThe agent front-end owns this section and parses it itself, leniently, straight out of the\nTOML document (`crates/basemind-tui/src/config.rs`). The core server never reads it — but it\nmust still ACCEPT it, because `deny_unknown_fields` above applies to the whole file: without\nthis passthrough, a repo whose `basemind.toml` legitimately carries `[agent]` fails to parse,\nthe daemon cannot host that workspace (`host_build_failed`), `serve` dies before `initialize`,\nand every MCP tool silently disappears. Keeping it untyped avoids duplicating the agent\ncrate's role schema here while preserving strict typo-checking for every other section."
    }
  },
  "additionalProperties": false,
  "required": [
    "$schema"
  ],
  "$defs": {
    "ScanConfig": {
      "type": "object",
      "properties": {
        "include": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "default": [
            "**/*"
          ]
        },
        "exclude": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "default": [
            "**/target/**",
            "**/node_modules/**",
            "**/dist/**",
            "**/.venv/**",
            "**/.basemind/**",
            "**/.git/**",
            "**/bazel-out/**",
            "**/bazel-bin/**",
            "**/bazel-testlogs/**",
            "**/bazel-*/**"
          ]
        },
        "respect_gitignore": {
          "type": "boolean",
          "default": true
        },
        "follow_symlinks": {
          "description": "Follow symlinks during the repository walk. Default `false` — symlinks are a common way to\nescape the repo (Bazel's `bazel-*` convenience symlinks point into an external output tree),\nand following them can balloon the scan or pull in unrelated files. Set `true` for repos that\ndeliberately symlink real source into place; the exclude floor still prunes `bazel-*` so a\nsymlinked Bazel tree does not leak in. `extra_roots` always follow symlinks regardless of this\nflag (Bazel `external/` is symlink-heavy and is opted into explicitly).",
          "type": "boolean",
          "default": false
        },
        "max_file_bytes": {
          "description": "Skip files larger than this. Prevents minified-bundle stalls.",
          "type": "integer",
          "format": "uint64",
          "minimum": 1024,
          "default": 2097152
        },
        "skip_submodules": {
          "description": "When true (default), the scanner skips paths under any submodule root listed in\n`.gitmodules`. Set to false to recurse into submodule working trees — useful only\nif you want one combined index across the parent + its embedded repos.",
          "type": "boolean",
          "default": true
        },
        "eager_l2": {
          "description": "When true (default), the scanner runs L2 extraction (calls + docs) inline with L1.\nL2 populates the `calls_by_callee` Fjall partition that drives the `code` tool's\n`references` and `callers` modes. Flipping to `false` halves the scan budget on large\nrepos at the cost\nof empty reference-search results until a foreground L2 pass is triggered (or the\nexisting on-demand `query::file_outline_l2` lazy path runs).",
          "type": "boolean",
          "default": true
        },
        "extra_roots": {
          "description": "Extra directories to index in addition to the repository root. Each entry is an\nabsolute path to a directory *outside* the repo — e.g. a Bazel external repo cache\n(`/private/var/tmp/_bazel_<user>/<hash>/external`) — whose files should resolve in\nsymbol search, references, outlines, and document search.\n\nFiles under an extra root are keyed by their **absolute** path (repo files stay\nrepo-relative), so returned paths for external files are absolute. Missing or\nunreadable roots are skipped with a warning; a root inside the repo is ignored (the\nprimary walk already covers it). Extra roots are (re-)indexed on a full `basemind\nscan` only — the live watcher does not track them. Symlinks are followed for extra\nroots (Bazel `external/` is symlink-heavy). Because these trees can be large, scope\nthem narrowly and lean on `exclude` + `max_file_bytes`.",
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": []
        }
      },
      "additionalProperties": false
    },
    "CodeIntelConfig": {
      "type": "object",
      "properties": {
        "precise_resolution": {
          "description": "Master switch for precise, scope- and import-aware name resolution. When `true` (default),\nlanguages with a precise engine resolve references to their true definitions — JS/TS via oxc\n(`code-intel-js`), Python/Java via the stack-graphs `.tsg` engine (`code-intel-stack`) — so\n`code` modes `references` / `callers` / `definition` distinguish a shadowed local from an\nimport instead of matching by name. Set to `false` to fall back to fast tree-sitter `locals`\nscope binding for every language (the precise engines are skipped). Inert for languages with\nno precise engine regardless. Takes effect on files (re)analyzed after the change; run a full\n`basemind scan` to apply it to an already-indexed repo.",
          "type": "boolean",
          "default": true
        }
      },
      "additionalProperties": false
    },
    "WatchConfig": {
      "type": "object",
      "properties": {
        "debounce_ms": {
          "description": "Coalesce file events within this window (milliseconds).",
          "type": "integer",
          "format": "uint64",
          "minimum": 0,
          "maximum": 60000,
          "default": 250
        },
        "live_l2": {
          "type": "boolean",
          "default": false
        }
      },
      "additionalProperties": false
    },
    "CacheConfig": {
      "type": "object",
      "properties": {
        "file_map_lru": {
          "description": "Maximum number of extracted FileMaps to keep hot in memory.",
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "default": 256
        }
      },
      "additionalProperties": false
    },
    "McpConfig": {
      "type": "object",
      "properties": {
        "transport": {
          "$ref": "#/$defs/McpTransport",
          "default": "stdio"
        }
      },
      "additionalProperties": false
    },
    "McpTransport": {
      "type": "string",
      "enum": [
        "stdio"
      ]
    },
    "LanguageConfig": {
      "type": "object",
      "properties": {
        "enabled": {
          "type": "boolean",
          "default": true
        }
      },
      "additionalProperties": false
    },
    "DocumentsConfig": {
      "description": "Top-level `[documents]` table. Each sub-config has `#[serde(default)]` so\nadding a new tier never breaks older TOML files.",
      "type": "object",
      "properties": {
        "enabled": {
          "description": "Master switch. Only meaningful when the `documents` cargo feature is compiled in.",
          "type": "boolean",
          "default": true
        },
        "mime_allowlist": {
          "description": "MIME-type allowlist. Empty = accept anything xberg can handle.",
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "default": []
        },
        "extension_denylist": {
          "description": "Extra file extensions (lowercase, no dot) to skip on top of the built-in archive/binary\ndenylist. The built-in floor (`.zip/.tar/.jar/.so/.wasm/...`) is always applied — this only\nadds to it — so archives and binaries are never routed to xberg extraction + embedding.",
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "default": []
        },
        "max_chunks_per_document": {
          "description": "Hard cap on chunks embedded per document. A pathological input (huge generated file, an\narchive that slipped the denylist) can otherwise explode into tens of thousands of ONNX\nembeds. Documents exceeding the cap are extracted but not embedded (blob written, no vector\nrows) with a warning.",
          "type": "integer",
          "format": "uint",
          "minimum": 1,
          "default": 2000
        },
        "max_characters": {
          "description": "Maximum chunk size in characters.",
          "type": "integer",
          "format": "uint",
          "minimum": 64,
          "default": 1000
        },
        "overlap": {
          "description": "Overlap between chunks in characters.",
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "default": 200
        },
        "embedding_preset": {
          "description": "Xberg embedding preset name. Defaults to \"balanced\".",
          "type": "string",
          "default": "balanced"
        },
        "embed": {
          "description": "Generate embeddings (`true`) or skip vector storage entirely (`false`).",
          "type": "boolean",
          "default": true
        },
        "embed_exclude": {
          "description": "Glob patterns (repo-relative, forward-slash) for documents that are still extracted +\nindexed but **never** embedded. Keeps ONNX vectors off selected large or low-value documents\nwhile leaving their text searchable by keyword. Empty by default; only consulted when\n`embed = true`.",
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "default": []
        },
        "extract_archives": {
          "description": "Route archives (`.zip/.tar/.jar/...`) into xberg's recursive archive extractor. Default\n`false`: archives and compressed containers are rejected by the built-in floor before any\nextraction, so a single archive can't explode into thousands of embeds. Flip to `true` to opt\ninto extracting the files *inside* archives — at the cost of the extra recursion + embedding\nwork. True binaries (`.so/.wasm/.exe/...`) are always rejected regardless of this flag.",
          "type": "boolean",
          "default": false
        },
        "language": {
          "description": "Language detection + preferred languages for chunking / extraction.",
          "$ref": "#/$defs/DocLanguageConfig",
          "default": {
            "auto_detect": true,
            "min_confidence": 0.8,
            "detect_multiple": false,
            "preferred_languages": []
          }
        },
        "reranker": {
          "description": "Cross-encoder reranker applied post-vector-search.",
          "$ref": "#/$defs/RerankerConfig",
          "default": {
            "enabled": false,
            "preset": "bge-reranker-base",
            "top_k": 10
          }
        },
        "keywords": {
          "description": "Keyword extraction at ingest time.",
          "$ref": "#/$defs/KeywordsConfig",
          "default": {
            "enabled": false,
            "algorithm": "yake",
            "max_keywords": 10,
            "min_score": 0.0,
            "ngram_range": [
              1,
              3
            ],
            "yake_params": null,
            "rake_params": null
          }
        },
        "ner": {
          "description": "Named-entity recognition at ingest time.",
          "$ref": "#/$defs/NerConfig",
          "default": {
            "enabled": false,
            "backend": "onnx",
            "model": null,
            "categories": [],
            "custom_labels": []
          }
        },
        "summarization": {
          "description": "Document summarization at ingest time.",
          "$ref": "#/$defs/SummarizationConfig",
          "default": {
            "enabled": false,
            "strategy": "extractive"
          }
        },
        "ocr": {
          "description": "OCR backend selection for image / scanned-PDF inputs.",
          "$ref": "#/$defs/OcrConfig",
          "default": {
            "backend": "tesseract",
            "languages": [
              "eng"
            ]
          }
        },
        "output": {
          "description": "Response wire format for the document MCP tools.",
          "$ref": "#/$defs/OutputConfig",
          "default": {
            "format": "json"
          }
        },
        "embed_max_threads": {
          "description": "Maximum number of threads dedicated to ONNX embedding. `0` means\n\"auto = max(2, logical_cpus / 4)\" — a bounded fraction of available\ncores so the embedder never pins the machine. Both the document and\ncode-search tiers route their `embed_texts` calls through a single\nprocess-wide rayon `ThreadPool` of this size; code-map extraction\nkeeps the full global pool.",
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "default": 0
        }
      },
      "additionalProperties": false
    },
    "DocLanguageConfig": {
      "description": "Language detection knobs for the document tier. Named `DocLanguageConfig` to\navoid colliding with the per-tree-sitter-language `LanguageConfig` (which is\nthe scanner's per-grammar override map).\n\nXberg drives language detection through the `whatlang` crate and reports\nISO 639-3 codes (three letters, e.g. `\"fra\"`, `\"deu\"`) — xberg's own\n`ExtractionResult.detected_languages` doc-comment mislabels them as ISO\n639-1 in rc.10, but the wrapper at `xberg::language_detection` normalises\nevery `whatlang` enum variant to its ISO 639-3 form. The `auto_detect`,\n`min_confidence`, and `detect_multiple` knobs map straight through to\nxberg's `LanguageDetectionConfig`. `preferred_languages` is reserved for\nfuture use — xberg rc.10 does not honor a preferred-language hint, so\nthe field is plumbed but inert today; we keep it on the schema so callers\ncan start populating it without a config break when support lands.",
      "type": "object",
      "properties": {
        "auto_detect": {
          "description": "Run xberg's language detector. On by default; flip off when every doc\nin the corpus is the same known language.",
          "type": "boolean",
          "default": true
        },
        "min_confidence": {
          "description": "Minimum detector confidence (0.0–1.0). Detections below this threshold\nare dropped. Matches xberg's default of 0.8.",
          "type": "number",
          "format": "double",
          "minimum": 0.0,
          "maximum": 1.0,
          "default": 0.8
        },
        "detect_multiple": {
          "description": "When true, xberg reports every language detected in the document\ninstead of just the top match. Off by default to match xberg.",
          "type": "boolean",
          "default": false
        },
        "preferred_languages": {
          "description": "Reserved — accepts ISO 639-3 codes (e.g. `\"fra\"`, `\"deu\"`) for future use.\nXberg rc.10 does not honor a preferred-language hint, but the field\nis kept on the schema so users can populate it without a config break.",
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": []
        }
      },
      "additionalProperties": false
    },
    "RerankerConfig": {
      "type": "object",
      "properties": {
        "enabled": {
          "description": "Master switch — off by default; the reranker model download + per-query\nlatency means users should opt in explicitly.",
          "type": "boolean",
          "default": false
        },
        "preset": {
          "description": "Xberg reranker preset name (`bge-reranker-base` is the small default;\n`bge-reranker-large` and `bge-reranker-v2-m3` are heavier alternatives).",
          "type": "string",
          "default": "bge-reranker-base"
        },
        "top_k": {
          "description": "How many hits to rerank. The vector search returns `top_k` candidates\nwhich the cross-encoder then reorders.",
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "default": 10
        }
      },
      "additionalProperties": false
    },
    "KeywordsConfig": {
      "type": "object",
      "properties": {
        "enabled": {
          "description": "Master switch — off by default; YAKE / RAKE add ingest-time CPU cost.\nMaps to `Some(KeywordConfig)` / `None` on `ExtractionConfig.keywords`;\nxberg's own `KeywordConfig` has no `enabled` field — gating is via\nthe wrapping `Option`.",
          "type": "boolean",
          "default": false
        },
        "algorithm": {
          "description": "Algorithm: YAKE (statistical, multi-language) or RAKE (rapid automatic\nkeyword extraction).",
          "$ref": "#/$defs/KeywordAlgorithm",
          "default": "yake"
        },
        "max_keywords": {
          "description": "Maximum keywords to extract per document. Matches xberg's\n`KeywordConfig.max_keywords` default of 10.",
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "default": 10
        },
        "min_score": {
          "description": "Minimum score threshold. Matches xberg's `KeywordConfig.min_score`\ndefault of 0.0 (i.e. surface every candidate). Score ranges differ\nbetween YAKE (lower = better) and RAKE (higher = better) — see\n`xberg::keywords::config::KeywordConfig.min_score`.",
          "type": "number",
          "format": "float",
          "minimum": 0.0,
          "default": 0.0
        },
        "ngram_range": {
          "description": "N-gram range as `[min, max]`. Matches xberg's\n`KeywordConfig.ngram_range` default of `(1, 3)`. Encoded as an array of\nlength 2 so the JSON Schema stays human-readable; values map back to a\n`(usize, usize)` tuple at the boundary.",
          "type": "array",
          "items": {
            "type": "integer",
            "format": "uint",
            "minimum": 0
          },
          "minItems": 2,
          "maxItems": 2,
          "default": [
            1,
            3
          ]
        },
        "yake_params": {
          "description": "Optional YAKE tuning (passed through to xberg unchanged). Shape\nmatches `xberg::keywords::YakeParams`; bad JSON is logged and\nxberg's defaults are used instead of failing the scan.",
          "default": null
        },
        "rake_params": {
          "description": "Optional RAKE tuning (passed through to xberg unchanged). Shape\nmatches `xberg::keywords::RakeParams`; bad JSON is logged and\nxberg's defaults are used instead of failing the scan.",
          "default": null
        }
      },
      "additionalProperties": false
    },
    "KeywordAlgorithm": {
      "type": "string",
      "enum": [
        "yake",
        "rake"
      ]
    },
    "NerConfig": {
      "type": "object",
      "properties": {
        "enabled": {
          "description": "Master switch — off by default; ONNX backend downloads gline-rs weights on\nfirst use, LLM backend costs API tokens. Maps to `Some(NerConfig)` /\n`None` on `ExtractionConfig.ner`.",
          "type": "boolean",
          "default": false
        },
        "backend": {
          "description": "Backend selection. `onnx` uses gline-rs locally; `llm` routes through the\nshared `[llm]` config.",
          "$ref": "#/$defs/NerBackend",
          "default": "onnx"
        },
        "model": {
          "description": "Override the ONNX model name (xberg has a default catalogue when unset).",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "categories": {
          "description": "Categories to surface — matches xberg's `NerConfig.categories`.\nAccepted values are the lowercase forms `\"person\"`, `\"organization\"`,\n`\"location\"`, `\"date\"`, `\"time\"`, `\"money\"`, `\"percent\"`, `\"email\"`,\n`\"phone\"`, `\"url\"`; anything else becomes a `Custom(_)` category at the\nboundary. Empty means \"use the backend default\".",
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": []
        },
        "custom_labels": {
          "description": "Arbitrary user-supplied entity labels for gline-rs zero-shot inference\n(and the LLM backend's structured-output schema). Matches xberg's\n`NerConfig.custom_labels`. Useful for domain-specific types like\n`\"Treatment\"`, `\"Vessel\"` without forking GLiNER's taxonomy. Custom\nlabels surface as `EntityCategory::Custom(_)` in the entity stream.",
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": []
        }
      },
      "additionalProperties": false
    },
    "NerBackend": {
      "type": "string",
      "enum": [
        "onnx",
        "llm"
      ]
    },
    "SummarizationConfig": {
      "type": "object",
      "properties": {
        "enabled": {
          "description": "Master switch — off by default. When true, the boundary code populates\n`ExtractionConfig.summarization`; the resulting `ExtractionResult.summary`\nsurfaces on `FileMapDoc.summary` and `DocumentSearchHit.summary`.",
          "type": "boolean",
          "default": false
        },
        "strategy": {
          "description": "Strategy: `extractive` (TextRank, no LLM) or `abstractive` (routed via\nthe top-level `[llm]` config). When `abstractive` is set but `[llm].model`\nis empty, the boundary falls back to `extractive` with a warning so the\nscan still completes.",
          "$ref": "#/$defs/SummarizationStrategy",
          "default": "extractive"
        },
        "max_tokens": {
          "description": "Soft cap on summary length in tokens. `None` lets xberg pick a default\nsuited to the chosen strategy (the xberg-side type uses the same\n`Option<u32>` shape; pass-through with no policy of our own).",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        }
      },
      "additionalProperties": false
    },
    "SummarizationStrategy": {
      "type": "string",
      "enum": [
        "extractive",
        "abstractive"
      ]
    },
    "OcrConfig": {
      "type": "object",
      "properties": {
        "backend": {
          "description": "Backend selection. `tesseract` is the default; `paddle` for CJK-heavy\ncorpora; `vlm` routes through `[llm]` for vision-language OCR.",
          "$ref": "#/$defs/OcrBackend",
          "default": "tesseract"
        },
        "languages": {
          "description": "Tesseract / PaddleOCR language packs (ISO 639-3 codes like `\"eng\"`).",
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [
            "eng"
          ]
        }
      },
      "additionalProperties": false
    },
    "OcrBackend": {
      "type": "string",
      "enum": [
        "tesseract",
        "paddle",
        "vlm"
      ]
    },
    "OutputConfig": {
      "type": "object",
      "properties": {
        "format": {
          "description": "Wire format used by document-tier MCP responses.",
          "$ref": "#/$defs/OutputFormat",
          "default": "json"
        }
      },
      "additionalProperties": false
    },
    "OutputFormat": {
      "type": "string",
      "enum": [
        "json",
        "toon"
      ]
    },
    "ResourcesConfig": {
      "description": "Top-level `[resources]` table. Every field has `#[serde(default)]` (directly\nor via a default fn) so adding a knob never breaks an older TOML file, and an\nomitted `[resources]` section deserialises to [`ResourcesConfig::default`].\n\n`0` is the \"auto\" sentinel for the thread / concurrency caps: it means \"let\nbasemind pick a bounded fraction of the machine\" rather than \"use zero\nthreads\". This keeps the default config safe on a laptop while letting an\noperator pin an explicit budget on a shared box.",
      "type": "object",
      "properties": {
        "scan_threads": {
          "description": "Cap on the code-map scanner's rayon pool. `0` (auto) keeps rayon's\ndefault (one worker per logical CPU); a non-zero value pins the pool so\nthe scan can't saturate every core on a shared machine. First scan wins\nfor the process — the pool is built once and its size is then fixed.",
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "default": 0
        },
        "embed_threads": {
          "description": "Cap on the ONNX embedding pool. `0` (auto) resolves to\n`max(2, logical_cpus / 4)` via `crate::embeddings::resolve_embed_threads`\n— a bounded fraction of cores so the embedder never pins the machine and\nORT arenas are not replicated across every core. This supersedes the\ndeprecated `[documents].embed_max_threads`; see\n[`ResourcesConfig::effective_embed_threads`] for the precedence.",
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "default": 0
        },
        "max_concurrent_documents": {
          "description": "Upper bound on documents extracted concurrently. `0` (auto) leaves the\ndispatch unbounded (today's behaviour). Parsed now; a concurrency\nsemaphore around document dispatch is a later iteration — the field\nexists so the schema is stable ahead of the consumer. Memory is bounded\ntoday by `max_footprint_mb` rather than by an in-flight document count.",
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "default": 0
        },
        "embed_batch_size": {
          "description": "Number of chunks the embedder submits to ONNX per batch. Larger batches\namortise per-call overhead at the cost of a higher transient memory\nspike; 32 is a safe default across the preset models. Threaded into both\n`SharedEmbedder` (code-search + query paths) and the document extractor's\n`EmbeddingConfig`.",
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "default": 32
        },
        "max_footprint_mb": {
          "description": "Hard ceiling on process physical footprint in mebibytes. `0` (disabled)\nis the default. When set, the best-effort backpressure gate\n([`crate::backpressure::FootprintGate`]) samples\n[`crate::sysres::phys_footprint`] at the document-extraction and\nchunk-embedding admit points and parks the worker while the process is\nover this ceiling, shaving the scan's peak. It is best-effort: an\nunreadable sample or a sustained overshoot admits anyway to guarantee\nforward progress, so the ceiling shapes the peak rather than enforcing a\nhard invariant. macOS today (`phys_footprint` is a Darwin metric); a\nno-op elsewhere.",
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "default": 0
        },
        "document_models": {
          "description": "Which model families run during document extraction. `Full` (default)\nruns every configured post-processor; the narrower profiles strip\nenrichment / embeddings to shrink the scan-time footprint on code-centric\nworkspaces. See [`DocumentModelProfile`].",
          "$ref": "#/$defs/DocumentModelProfile",
          "default": "full"
        }
      },
      "additionalProperties": false
    },
    "DocumentModelProfile": {
      "description": "Selects which model families run during document extraction, trading recall\nfor a smaller scan-time footprint on workspaces that are mostly source code.\n\nThe enrichment post-processors (keyword extraction, NER, summarisation) and\nOCR each pull in their own ONNX / LLM weights; a code workspace rarely wants\nany of them. Narrowing the profile lets an operator keep the code map and\n(optionally) embeddings while paying for nothing else.",
      "oneOf": [
        {
          "description": "Run every configured capability (embeddings + keywords + NER +\nsummarisation + OCR). The default — no behaviour change from before this\nknob existed.",
          "type": "string",
          "const": "full"
        },
        {
          "description": "Embeddings only: chunks are still embedded per the `[documents]` config,\nbut keyword extraction, NER, and summarisation are forced off and OCR is\ndisabled. The lever for a code-centric workspace that still wants\nsemantic document search.",
          "type": "string",
          "const": "code_only"
        },
        {
          "description": "Metadata only: no embeddings and no enrichment post-processors, OCR\ndisabled. Documents are extracted for their text + metadata (keyword\nsearch) but never routed to any model. Serialised as `\"none\"` (the Rust\nidentifier carries a trailing underscore only because `None` collides\nwith `Option::None`).",
          "type": "string",
          "const": "none"
        }
      ]
    },
    "CodeSearchConfig": {
      "description": "`[code_search]` table. Chunk + embed source code for the `code` tool's `semantic` mode.",
      "type": "object",
      "properties": {
        "enabled": {
          "description": "Master switch. Only meaningful when the `code-search` cargo feature is compiled in.\nDefault `true` — a `code-search` build chunks source + builds the BM25 keyword lane on scan\nunless disabled. Embedding is a separate opt-in gated by `embed` (off by default).",
          "type": "boolean",
          "default": true
        },
        "max_characters": {
          "description": "Maximum chunk size in characters. Chunks longer than this are split into overlapping\nwindows. Mirrors the document tier's `max_characters`.",
          "type": "integer",
          "format": "uint",
          "minimum": 64,
          "default": 1500
        },
        "overlap": {
          "description": "Overlap between split windows, in characters. Mirrors the document tier's `overlap`.",
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "default": 200
        },
        "embed": {
          "description": "Generate embeddings (`true`) or chunk-only without vector storage (`false`). **Default\n`false`**: local embeddings on *code* aren't worth their cost — code is embedded with a\ngeneral English model and the one real win (NL→symbol) is already served by the BM25 keyword\nlane over the same text. With embeddings off the `.chunk.msgpack` cache is still written and\nthe BM25 keyword lane still works (so `lane: \"keyword\"` functions), but no LanceDB\nrows land — the semantic (vector) lane returns nothing and no ONNX model is downloaded. Flip\nto `true` only if you specifically want vector search over code.",
          "type": "boolean",
          "default": false
        },
        "embed_exclude": {
          "description": "Glob patterns (repo-relative, forward-slash) for files that are still chunked + indexed\n(BM25 / code-map) but **never** embedded. Use it to keep vectors off large generated or\nvendored files while leaving them searchable by keyword. Empty by default. Only consulted\nwhen `embed = true` — with embedding already off it is a no-op.",
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "default": []
        },
        "reranker": {
          "description": "Optional cross-encoder rerank of the fused `semantic` hits. Reuses the same xberg reranker\nas the documents tier. Off by default — the first call downloads an ONNX model. Enable via\n`[code_search.reranker] enabled = true` or the per-query `rerank_enabled` override.",
          "$ref": "#/$defs/RerankerConfig",
          "default": {
            "enabled": false,
            "preset": "bge-reranker-base",
            "top_k": 10
          }
        }
      },
      "additionalProperties": false
    },
    "MemoryConfig": {
      "type": "object",
      "properties": {
        "enabled": {
          "description": "Master switch. Only meaningful when the `memory` cargo feature is compiled in.",
          "type": "boolean",
          "default": true
        },
        "scope_strategy": {
          "description": "How to derive the scope key for an opened repository.",
          "$ref": "#/$defs/MemoryScopeStrategy",
          "default": "git_remote_with_fallback"
        },
        "default_visibility": {
          "description": "Default memory tier when a `memory_*` call omits `visibility`. `group` (shared) keeps\ntoday's behavior; set to `individual` so a user's writes default to their private tier.",
          "type": "string",
          "enum": [
            "group",
            "individual"
          ],
          "default": "group"
        }
      },
      "additionalProperties": false
    },
    "MemoryScopeStrategy": {
      "oneOf": [
        {
          "description": "Use normalized `origin` remote URL when available, else fall back to the\nworkdir realpath. This is the recommended default — clones of the same\nrepo share memory across machines.",
          "type": "string",
          "const": "git_remote_with_fallback"
        },
        {
          "description": "Always use the workdir realpath. Separates clones of the same repo.",
          "type": "string",
          "const": "workdir_only"
        }
      ]
    },
    "CommsConfig": {
      "description": "`[comms]` config sub-tree. See module docs for the lifecycle context.",
      "type": "object",
      "properties": {
        "enabled": {
          "description": "Master switch. Only meaningful when the `comms` cargo feature is compiled in; when\n`false` the MCP / CLI comms tools are wired but the server never connects to a daemon.",
          "type": "boolean",
          "default": true
        },
        "agent_id": {
          "description": "Stable identity this process presents to the broker. When unset, the identity resolver\nfalls back to `BASEMIND_AGENT_ID`, then a generated-and-persisted per-workspace id, then a\nprocess-unique generated id. It never falls back to a shared constant: two agents that\ncollapse onto one id also share one inbox, and each then sees the other's messages as its\nown. Validated through [`crate::comms::ids::AgentId`] at resolution time.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "idle_timeout_secs": {
          "description": "Seconds of zero-subscriber idle before the broker sheds caches. Keeps the socket bound.",
          "type": "integer",
          "format": "uint64",
          "minimum": 1,
          "default": 1800
        },
        "max_messages_per_room": {
          "description": "Per-room front-matter retention cap before old messages become prune-eligible.",
          "type": "integer",
          "format": "uint32",
          "minimum": 1,
          "default": 1000
        },
        "retention_secs": {
          "description": "Message retention window in seconds. Older messages are eligible for pruning.",
          "type": "integer",
          "format": "uint64",
          "minimum": 1,
          "default": 604800
        },
        "max_rooms": {
          "description": "Hard cap on the number of concurrently registered rooms.",
          "type": "integer",
          "format": "uint32",
          "minimum": 1,
          "default": 256
        },
        "workspace_root": {
          "description": "Optional explicit workspace root the daemon associates this client with. When unset the\nclient uses its discovered repo / cwd for scope context.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      },
      "additionalProperties": false
    },
    "CrawlConfig": {
      "type": "object",
      "properties": {
        "respect_robots_txt": {
          "description": "Honour `robots.txt` when fetching pages. Default `true`. Override only\nfor hosts you control — flipping this off violates `robots.txt`\ndirectives for every domain the crawler touches.",
          "type": "boolean",
          "default": true
        },
        "max_pages": {
          "description": "Hard cap on pages visited in a single `web_crawl` call.",
          "type": "integer",
          "format": "uint32",
          "minimum": 1,
          "default": 32
        },
        "max_depth": {
          "description": "Maximum link-following depth from the seed URL during `web_crawl`.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "default": 2
        },
        "max_body_size": {
          "description": "Truncate response bodies above this many bytes before parsing.",
          "type": "integer",
          "format": "uint64",
          "minimum": 1024,
          "default": 4194304
        },
        "user_agent": {
          "description": "User-Agent header sent with every request. Override to identify your\ncrawler to operators; the default includes the basemind release\nversion + the upstream repo URL so site operators can trace traffic.",
          "type": "string",
          "minLength": 1,
          "default": "basemind/0.23.1 (+https://github.com/Goldziher/basemind)"
        },
        "allow_private_network": {
          "description": "Allow crawling URLs that resolve to private, loopback, or link-local\naddresses (`127.0.0.0/8`, `10.0.0.0/8`, `169.254.0.0/16`, …). Default\n`false`: the engine rejects them with an SSRF-policy violation. Flip this\non only to scrape an internal docs server you control. (The\n`CRAWLBERG_ALLOW_PRIVATE_NETWORK` env var is honoured as a process-wide\noverride regardless of this setting.)",
          "type": "boolean",
          "default": false
        }
      },
      "additionalProperties": false
    },
    "ShellsConfig": {
      "description": "`[shells]` config sub-tree. See module docs for context.",
      "type": "object",
      "properties": {
        "enabled": {
          "description": "Master switch. Only meaningful when the `shells` cargo feature is compiled in; when\n`false` the shell MCP / CLI tools are wired but no session is ever spawned.",
          "type": "boolean",
          "default": true
        },
        "visual": {
          "description": "How a visual session is surfaced. Defaults to [`VisualMode::Current`] — a new tab in the\nterminal that is already open.",
          "$ref": "#/$defs/VisualMode",
          "default": "current"
        },
        "terminal": {
          "description": "Which terminal emulator to drive. [`TerminalChoice::Auto`] (the default) detects the\nrunning terminal from the environment and falls back gracefully.",
          "$ref": "#/$defs/TerminalChoice",
          "default": "auto"
        },
        "default_cols": {
          "description": "Initial column count for the session's pseudo-terminal.",
          "type": "integer",
          "format": "uint16",
          "minimum": 1,
          "maximum": 65535,
          "default": 200
        },
        "default_rows": {
          "description": "Initial row count for the session's pseudo-terminal.",
          "type": "integer",
          "format": "uint16",
          "minimum": 1,
          "maximum": 65535,
          "default": 50
        },
        "keep_on_exit": {
          "description": "Keep the session alive after the visual surface is closed. When `false` the session is\ntorn down once its presentation exits.",
          "type": "boolean",
          "default": true
        }
      },
      "additionalProperties": false
    },
    "VisualMode": {
      "description": "How a visual shell session is presented to the user.",
      "oneOf": [
        {
          "description": "Open the session as a new tab in the terminal that is already open. The default — it keeps\nthe user's window layout intact.",
          "type": "string",
          "const": "current"
        },
        {
          "description": "Open the session in a brand-new terminal window.",
          "type": "string",
          "const": "window"
        },
        {
          "description": "Surface the session over a web frontend (wired by the lead; command-building is a no-op).",
          "type": "string",
          "const": "web"
        },
        {
          "description": "Do not present the session at all; it runs headless.",
          "type": "string",
          "const": "headless"
        }
      ]
    },
    "TerminalChoice": {
      "description": "Which terminal emulator the visual launcher should drive.\n\n[`TerminalChoice::Auto`] inspects the environment (`TERM_PROGRAM`, `$TERMINAL`, and platform\ndefaults) to pick a concrete emulator. The explicit variants force a specific emulator\nregardless of detection.",
      "oneOf": [
        {
          "description": "Detect the running terminal from the environment and fall back gracefully.",
          "type": "string",
          "const": "auto"
        },
        {
          "description": "iTerm2 (macOS).",
          "type": "string",
          "const": "iterm2"
        },
        {
          "description": "Terminal.app (macOS).",
          "type": "string",
          "const": "terminal_app"
        },
        {
          "description": "Windows Terminal (`wt.exe`).",
          "type": "string",
          "const": "windows_terminal"
        },
        {
          "description": "GNOME Terminal (Linux).",
          "type": "string",
          "const": "gnome_terminal"
        },
        {
          "description": "Konsole (Linux / KDE).",
          "type": "string",
          "const": "konsole"
        },
        {
          "description": "WezTerm (cross-platform).",
          "type": "string",
          "const": "wezterm"
        },
        {
          "description": "Alacritty (cross-platform).",
          "type": "string",
          "const": "alacritty"
        },
        {
          "description": "kitty (cross-platform).",
          "type": "string",
          "const": "kitty"
        },
        {
          "description": "xterm (Linux / X11 fallback).",
          "type": "string",
          "const": "xterm"
        }
      ]
    },
    "LlmConfig": {
      "description": "Shared LLM credentials + model selection. Consumed by every LLM-backed\ncapability (ner-llm, summarization-llm, reranker-llm, VLM OCR).\n\nMirrors xberg's `LlmConfig` field-for-field with one safety upgrade:\n`api_key` is the [`ApiKey`] tri-state (literal / env-ref / unset) instead of\n`Option<String>`, so credentials are never stored as bare literals in TOML.\nAt the boundary the env-ref is resolved into a [`SecretString`] and then\nexposed via [`SecretString::expose`] when constructing xberg's struct.",
      "type": "object",
      "properties": {
        "model": {
          "description": "Combined provider/model string following liter-llm's routing format:\n`\"openai/gpt-4o\"`, `\"anthropic/claude-sonnet-4-20250514\"`,\n`\"groq/llama-3.1-70b-versatile\"`. Empty string ⇒ inert; every LLM-backed\nfeature treats an empty `model` as \"no LLM configured\".",
          "type": "string",
          "default": ""
        },
        "api_key": {
          "description": "API key — either a literal (discouraged; keeps secrets in version control)\nor an `{ env = \"OPENAI_API_KEY\" }` reference. `Unset` lets the underlying\nprovider SDK fall back to its standard environment variable lookup.",
          "$ref": "#/$defs/ApiKey",
          "default": null
        },
        "base_url": {
          "description": "Override the provider base URL (for self-hosted vLLM, Azure OpenAI, …).",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "temperature": {
          "description": "Sampling temperature. Provider-default when unset.",
          "type": [
            "number",
            "null"
          ],
          "format": "double",
          "default": null
        },
        "timeout_secs": {
          "description": "Request timeout in seconds. Maps to xberg's `timeout_secs` (default 60).",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0,
          "default": null
        },
        "max_retries": {
          "description": "Maximum retry attempts on transient errors. Maps to xberg's\n`max_retries` (default 3).",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0,
          "default": null
        },
        "max_tokens": {
          "description": "Maximum tokens to generate. Provider-default when unset.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0,
          "default": null
        }
      },
      "additionalProperties": false
    },
    "ApiKey": {
      "description": "Tri-state API key wrapper. Serde-deserialised as `null` (`Unset`), a plain\nstring (`Literal`), or a `{ env = \"NAME\" }` table (`Env`).\n\n`Serialize` is implemented manually — the `Literal` variant NEVER emits its\ncleartext secret. It serialises to the marker string `\"<redacted>\"` so any\ndownstream pipeline that round-trips this enum through JSON / TOML (e.g.\nthe config validator at `src/config/mod.rs`) cannot leak credentials into\nlogs, snapshot tests, or error messages. Deserialisation is unchanged:\nloading a TOML config with a literal `api_key = \"sk-...\"` still produces\n`ApiKey::Literal(...)`. The redaction is one-way by design.",
      "anyOf": [
        {
          "description": "Literal value baked into config. Strongly discouraged for production —\n`Env` keeps secrets out of source control.",
          "type": "string"
        },
        {
          "description": "`{ env = \"OPENAI_API_KEY\" }` — resolved at load time via `std::env::var`.",
          "type": "object",
          "properties": {
            "env": {
              "type": "string"
            }
          },
          "required": [
            "env"
          ]
        },
        {
          "description": "Missing / `null` — every LLM-backed feature treats this as \"disabled\".",
          "type": "null"
        }
      ]
    }
  }
}
